const signUpButton = document.getElementsByClassName("buttonz");
const container = document.getElementsByClassName("container");

signUpButton.addEventListener('click', () => container.classList.add('form group'))

